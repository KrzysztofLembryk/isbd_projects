# Technology
Project is written in rust

# How to build
In folder with Cargo.toml (root folder of file_format_prep) run

```bash
# To build project
cargo build

# To run sample main
cargo run
```

# Tests
Currently no working tests in tests folder (obsolete tests only)